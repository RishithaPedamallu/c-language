#include<stdio.h>
int main()
{
	char op;
	int a,b,c;;
	printf("enter operator");
	scanf("%c",&op);
	printf("enter a,b, values");
	scanf("%d%d",&a,&b);
	switch (op)
 	case'+':c=a+b;
		printf("addition is %d",c);
		break;
		case'-':c=a-b;
		printf("substraction is %d",c);
		break;
		case'*':c=a*b;
		printf("multiplcation is %d",c);
		break;
		case'/':c=a/b;
		printf("divistion is %d",c);
		break;
		case'%':c=a%b;
		printf("modules is %d",c);
		break;
		default:print("invalid operators");
		break;
		return 0;
	}
